# CRC-15
A 15 bit cyclic redundancy check algorithm. Detects modifications of raw data that has traveled through a network.

The program calculates a CRC-15 value for a given file which can also verify the correctness of a given file that already has a CRC-15 value appended to it.
